[package]
name = "neural-memory-domain"
version.workspace = true
edition.workspace = true
license.workspace = true

# PURE CRATE. This dependency list is the enforcement mechanism for the
# effect-free doctrine carried over from neuralcompose-mobile-core: no database,
# no async runtime, no clock, no uuid. scripts/check-purity.sh asserts it.
[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }
